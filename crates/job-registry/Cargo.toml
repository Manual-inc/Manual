[package]
name = "job-registry"
version.workspace = true
edition.workspace = true

[lib]
name = "job_registry"
path = "src/lib.rs"

[dependencies]
job.workspace = true
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
node.workspace = true
workflow.workspace = true
