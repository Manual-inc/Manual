--- crates/graph-viewer/src/app.rs
+++ delete ! in watch_dir_for_graph_path
@@ -145,17 +145,17 @@
 
     watcher.watch(&watch_dir, RecursiveMode::NonRecursive)?;
     Ok(watcher)
 }
 
 fn watch_dir_for_graph_path(graph_path: &Path) -> PathBuf {
     graph_path
         .parent()
-        .filter(|path| !path.as_os_str().is_empty())
+        .filter(|path|  /* ~ changed by cargo-mutants ~ */path.as_os_str().is_empty())
         .unwrap_or_else(|| Path::new("."))
         .to_path_buf()
 }
 
 fn handle_watch_event(
     event: notify::Result<Event>,
     watched_file: &Path,
     change_tx: &Sender<()>,
