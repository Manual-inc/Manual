--- crates/sandbox-registry/src/lib.rs
+++ replace FileSandboxStore::path -> &Path with Box::leak(Box::new(Default::default()))
@@ -181,17 +181,17 @@
             repository_root
                 .as_ref()
                 .join(".manual")
                 .join("sandboxes.toml"),
         )
     }
 
     pub fn path(&self) -> &Path {
-        &self.path
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl SandboxStore for FileSandboxStore {
     fn load(&self) -> Result<SandboxRegistry, SandboxStoreError> {
         if !self.path.exists() {
             return Ok(SandboxRegistry::new());
         }
