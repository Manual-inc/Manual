--- crates/sandbox/src/lib.rs
+++ replace == with != in compile_linux
@@ -522,17 +522,17 @@
         "--unshare-pid".to_string(),
         "--die-with-parent".to_string(),
         "--proc".to_string(),
         "/proc".to_string(),
         "--dev".to_string(),
         "/dev".to_string(),
     ];
 
-    if policy.network.mode == NetworkMode::Restricted {
+    if policy.network.mode != /* ~ changed by cargo-mutants ~ */ NetworkMode::Restricted {
         args.push("--unshare-net".to_string());
     }
 
     for path in LINUX_RUNTIME_READ_PATHS {
         let runtime_path = Path::new(path);
         if runtime_path.exists() {
             args.push("--ro-bind".to_string());
             args.push((*path).to_string());
