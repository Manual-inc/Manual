--- crates/workflow/src/lib.rs
+++ delete ! in validate_graph
@@ -281,17 +281,17 @@
     for (edge_index, edge) in edges.iter().enumerate() {
         if edge.source == edge.target {
             return Err(WorkflowError::SelfLoop {
                 edge_index,
                 node_id: edge.source.to_string(),
             });
         }
 
-        if !node_ids.contains(edge.source.as_str()) {
+        if  /* ~ changed by cargo-mutants ~ */node_ids.contains(edge.source.as_str()) {
             return Err(WorkflowError::MissingEndpoint {
                 edge_index,
                 endpoint: "source",
                 node_id: edge.source.to_string(),
             });
         }
 
         if !node_ids.contains(edge.target.as_str()) {
