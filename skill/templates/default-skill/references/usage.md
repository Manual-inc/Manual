# Skill Usage

This reference document travels with the bundled skill template.

## Intended platforms

- Codex-style skills with a `SKILL.md` entrypoint
- Claude-style agent skills with supplemental Markdown guidance

## Workspace flow

The `manual-skill` binary delegates validation work to the shared `cli` crate.
