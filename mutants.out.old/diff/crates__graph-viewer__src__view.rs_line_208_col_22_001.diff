--- crates/graph-viewer/src/view.rs
+++ replace + with * in GraphView::draw_node
@@ -200,17 +200,17 @@
 
         painter.circle_filled(position, NODE_RADIUS, fill);
         painter.circle_stroke(
             position,
             NODE_RADIUS,
             Stroke::new(2.0, Color32::from_rgb(234, 238, 244)),
         );
         painter.text(
-            position + vec2(0.0, NODE_RADIUS + 8.0),
+            position * /* ~ changed by cargo-mutants ~ */ vec2(0.0, NODE_RADIUS + 8.0),
             Align2::CENTER_TOP,
             abbreviate(&node.label, 24),
             FontId::proportional(13.0),
             Color32::from_rgb(234, 238, 244),
         );
     }
 }
 
