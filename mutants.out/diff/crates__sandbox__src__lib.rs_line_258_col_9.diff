--- crates/sandbox/src/lib.rs
+++ replace <impl fmt::Display for SandboxError>::fmt -> fmt::Result with Ok(Default::default())
@@ -250,30 +250,17 @@
         detail: String,
     },
     UnsupportedPlatform(String),
     Io(String),
 }
 
 impl fmt::Display for SandboxError {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        match self {
-            Self::BackendUnavailable {
-                backend,
-                platform,
-                detail,
-            } => write!(
-                f,
-                "sandbox backend {backend:?} is unavailable on {platform:?}: {detail}"
-            ),
-            Self::UnsupportedPlatform(platform) => {
-                write!(f, "unsupported sandbox platform: {platform}")
-            }
-            Self::Io(message) => write!(f, "sandbox process failed: {message}"),
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl std::error::Error for SandboxError {}
 
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub struct SandboxRunner {
     platform: Platform,
