--- crates/graph-viewer/src/lib.rs
+++ replace += with *= in PanOffset::pan_by
@@ -337,17 +337,17 @@
     }
 
     pub fn y(self) -> f32 {
         self.y
     }
 
     pub fn pan_by(&mut self, delta_x: f32, delta_y: f32) {
         self.x += delta_x;
-        self.y += delta_y;
+        self.y *= /* ~ changed by cargo-mutants ~ */ delta_y;
     }
 
     pub fn reset(&mut self) {
         *self = Self::default();
     }
 }
 
 impl Default for PanOffset {
