--- crates/graph-viewer/src/lib.rs
+++ replace Graph::nodes -> &[Node] with Vec::leak(Vec::new())
@@ -94,17 +94,17 @@
                 label: edge.label,
             });
         }
 
         Self::new(nodes, edges)
     }
 
     pub fn nodes(&self) -> &[Node] {
-        &self.nodes
+        Vec::leak(Vec::new()) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn edges(&self) -> &[Edge] {
         &self.edges
     }
 }
 
 fn validate_graph(nodes: &[Node], edges: &[Edge]) -> Result<(), GraphLoadError> {
