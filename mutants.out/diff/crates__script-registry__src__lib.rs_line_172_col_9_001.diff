--- crates/script-registry/src/lib.rs
+++ replace ScriptRegistry::iter -> impl Iterator<Item =&ScriptDefinition> with ::std::iter::once(Box::leak(Box::new(Default::default())))
@@ -164,17 +164,17 @@
         self.scripts.len()
     }
 
     pub fn is_empty(&self) -> bool {
         self.scripts.is_empty()
     }
 
     pub fn iter(&self) -> impl Iterator<Item = &ScriptDefinition> {
-        self.scripts.values()
+        ::std::iter::once(Box::leak(Box::new(Default::default()))) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub enum ScriptRegistryError {
     InvalidId(ScriptIdError),
     DuplicateId(ScriptId),
     UnknownId(ScriptId),
