--- crates/job/src/lib.rs
+++ replace == with != in Job::refresh_status
@@ -285,17 +285,17 @@
 
         Ok(())
     }
 
     fn refresh_status(&mut self) {
         if self
             .nodes
             .iter()
-            .any(|node| node.status == NodeRunStatus::Failed)
+            .any(|node| node.status != /* ~ changed by cargo-mutants ~ */ NodeRunStatus::Failed)
         {
             self.status = JobStatus::Failed;
             return;
         }
 
         if self
             .nodes
             .iter()
