--- crates/graph-viewer/src/lib.rs
+++ replace PanOffset::reset with ()
@@ -341,17 +341,17 @@
     }
 
     pub fn pan_by(&mut self, delta_x: f32, delta_y: f32) {
         self.x += delta_x;
         self.y += delta_y;
     }
 
     pub fn reset(&mut self) {
-        *self = Self::default();
+        () /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl Default for PanOffset {
     fn default() -> Self {
         Self { x: 0.0, y: 0.0 }
     }
 }
