--- crates/graph-viewer/src/lib.rs
+++ delete ! in validate_graph
@@ -112,17 +112,17 @@
 
     for node in nodes {
         if !ids.insert(node.id.clone()) {
             return Err(GraphLoadError::DuplicateNode(node.id.clone()));
         }
     }
 
     for (edge_index, edge) in edges.iter().enumerate() {
-        if !ids.contains(&edge.source) {
+        if  /* ~ changed by cargo-mutants ~ */ids.contains(&edge.source) {
             return Err(GraphLoadError::MissingEndpoint {
                 edge_index,
                 endpoint: "source",
                 node_id: edge.source.clone(),
             });
         }
 
         if !ids.contains(&edge.target) {
