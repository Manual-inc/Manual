--- crates/job/src/lib.rs
+++ replace JobNode::restore -> Result<Self, NodeIdError> with Ok(Default::default())
@@ -133,21 +133,17 @@
         }
     }
 
     pub fn restore(
         node_id: impl Into<String>,
         status: NodeRunStatus,
         attempts: u32,
     ) -> Result<Self, NodeIdError> {
-        Ok(Self {
-            node_id: NodeId::new(node_id)?,
-            status,
-            attempts,
-        })
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn node_id(&self) -> &NodeId {
         &self.node_id
     }
 
     pub fn status(&self) -> NodeRunStatus {
         self.status
