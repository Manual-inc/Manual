--- crates/graph-viewer/src/lib.rs
+++ replace Graph::edges -> &[Edge] with Vec::leak(vec![Default::default()])
@@ -98,17 +98,17 @@
         Self::new(nodes, edges)
     }
 
     pub fn nodes(&self) -> &[Node] {
         &self.nodes
     }
 
     pub fn edges(&self) -> &[Edge] {
-        &self.edges
+        Vec::leak(vec![Default::default()]) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 fn validate_graph(nodes: &[Node], edges: &[Edge]) -> Result<(), GraphLoadError> {
     let mut ids = BTreeSet::new();
 
     for node in nodes {
         if !ids.insert(node.id.clone()) {
