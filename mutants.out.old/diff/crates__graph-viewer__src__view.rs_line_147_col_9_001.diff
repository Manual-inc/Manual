--- crates/graph-viewer/src/view.rs
+++ replace GraphView::screen_position -> Option<Pos2> with Some(Default::default())
@@ -139,19 +139,17 @@
 
     fn screen_position(
         &self,
         center: Pos2,
         scale: f32,
         node_id: &str,
         layout: &GraphLayout,
     ) -> Option<Pos2> {
-        layout
-            .position(node_id)
-            .map(|point| center + vec2(point.x * scale, point.y * scale))
+        Some(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn draw_edge(
         &self,
         painter: &Painter,
         center: Pos2,
         scale: f32,
         edge: &Edge,
