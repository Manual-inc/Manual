--- crates/graph-viewer/src/lib.rs
+++ replace / with * in circular_layout
@@ -169,17 +169,17 @@
     }
 
     if node_count == 1 {
         positions.insert(graph.nodes[0].id.clone(), Point { x: 0.0, y: 0.0 });
         return GraphLayout { positions };
     }
 
     for (index, node) in graph.nodes.iter().enumerate() {
-        let angle = TAU * index as f32 / node_count as f32;
+        let angle = TAU * index as f32 * /* ~ changed by cargo-mutants ~ */ node_count as f32;
         positions.insert(
             node.id.clone(),
             Point {
                 x: angle.cos(),
                 y: angle.sin(),
             },
         );
     }
