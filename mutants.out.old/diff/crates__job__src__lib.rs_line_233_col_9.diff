--- crates/job/src/lib.rs
+++ replace Job::nodes -> &[JobNode] with Vec::leak(Vec::new())
@@ -225,17 +225,17 @@
         &self.input_json
     }
 
     pub fn status(&self) -> JobStatus {
         self.status
     }
 
     pub fn nodes(&self) -> &[JobNode] {
-        &self.nodes
+        Vec::leak(Vec::new()) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn node_status(&self, node_id: &str) -> Option<NodeRunStatus> {
         self.nodes
             .iter()
             .find(|node| node.node_id.as_str() == node_id)
             .map(JobNode::status)
     }
