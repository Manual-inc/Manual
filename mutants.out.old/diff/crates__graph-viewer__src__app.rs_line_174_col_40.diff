--- crates/graph-viewer/src/app.rs
+++ replace == with != in event_touches_path
@@ -166,17 +166,17 @@
             let _ = change_tx.send(());
             egui_ctx.request_repaint();
         }
         _ => {}
     }
 }
 
 fn event_touches_path(event: &Event, graph_path: &Path) -> bool {
-    event.paths.iter().any(|path| path == graph_path)
+    event.paths.iter().any(|path| path != /* ~ changed by cargo-mutants ~ */ graph_path)
 }
 
 fn absolute_path(path: PathBuf) -> PathBuf {
     if path.is_absolute() {
         return path;
     }
 
     std::env::current_dir()
