--- crates/graph-viewer/src/lib.rs
+++ replace SmoothZoom::displayed -> ZoomLevel with Default::default()
@@ -265,17 +265,17 @@
     pub fn new(zoom: ZoomLevel) -> Self {
         Self {
             displayed: zoom,
             target: zoom,
         }
     }
 
     pub fn displayed(self) -> ZoomLevel {
-        self.displayed
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn target(self) -> ZoomLevel {
         self.target
     }
 
     pub fn zoom_in(&mut self) {
         self.target.zoom_in();
