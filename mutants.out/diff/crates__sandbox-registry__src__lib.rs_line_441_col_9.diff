--- crates/sandbox-registry/src/lib.rs
+++ replace StoredFilesystemEntry::into_entry -> FilesystemEntry with Default::default()
@@ -433,20 +433,17 @@
     fn from_entry(entry: &FilesystemEntry) -> Self {
         Self {
             path: entry.path.clone(),
             access: entry.access.into(),
         }
     }
 
     fn into_entry(self) -> FilesystemEntry {
-        FilesystemEntry {
-            path: self.path,
-            access: self.access.into(),
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Debug, Clone, Copy, Serialize, Deserialize)]
 #[serde(rename_all = "kebab-case")]
 enum StoredFilesystemAccess {
     Read,
     Write,
