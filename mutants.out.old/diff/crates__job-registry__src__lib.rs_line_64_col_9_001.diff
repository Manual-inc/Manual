--- crates/job-registry/src/lib.rs
+++ replace JobRegistry::jobs_for_workflow -> impl Iterator<Item =&'a Job>+'a with ::std::iter::once(Box::leak(Box::new(Default::default())))
@@ -56,19 +56,17 @@
     pub fn iter(&self) -> impl Iterator<Item = &Job> {
         self.jobs.values()
     }
 
     pub fn jobs_for_workflow<'a>(
         &'a self,
         workflow_id: &'a str,
     ) -> impl Iterator<Item = &'a Job> + 'a {
-        self.jobs
-            .values()
-            .filter(move |job| job.workflow_id() == workflow_id)
+        ::std::iter::once(Box::leak(Box::new(Default::default()))) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub enum JobRegistryError {
     InvalidId(JobIdError),
     DuplicateId(JobId),
     UnknownId(JobId),
